<%= form_for @projects do |f| %>
	<% if @projects.errors.any? %>
 	<h2><%= pluralize(@projects.errors.count, "errors") %> prevented this projects from saving:</h2>
 	<ul>
 	<% @projects.errors.full_messages.each do |msg| %>
 	<li><%= msg %> </li>
 	<%end %>
 </ul>
<%end %> 

<%= f.label :title %> <br>
<%= f.text_field :title %>

<br>
<br>

<%= f.label :description %> <br>
<%= f.text_area :description %>

<br>
<br>

<%= f.label :link %> <br>
<%= f.text_field :link %>

<br>
<br>

<%= f.submit %>

<% end %>